import React from 'react'

const MapComponent = () => (
  <div className='Map'>
    {/* {this.state.locationFound &&
          <div className='map-container'>
            <h3 className={this.state.h3_name}>{this.state.finalResult.name}</h3>
            {this.state.mapvisible &&
              <iframe className='map' frameBorder='0' marginHeight='0' marginWidth='0' src={`https://www.google.com/maps/embed/v1/place?key=${MAP_API_KEY}&q=place_id:${this.state.place_id}`} />
            }
          </div>
        } */}
  </div>
)

export default MapComponent
